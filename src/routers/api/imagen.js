const express = require("express");
const router = express.Router();

const apiControllers = require("../../controllers/apiControlles/apiImage");

//router.get("/", apiControllers.listadoImagenes);

module.exports = router;
